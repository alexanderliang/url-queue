{
  "name": "url-queue",
  "version": "1.0.0",
  "description": "Job Queue whose workers fetch data from a URL and stores results in a database. REST API used for queueing new URL and retrieving data",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Liang",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.16.2",
    "body-parser": "^1.17.2",
    "express": "^4.15.4",
    "mongoose": "^4.11.7",
    "node-cron": "^1.2.1",
    "request": "^2.81.0"
  }
}
